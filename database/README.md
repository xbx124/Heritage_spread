# 数据库脚本说明

## 执行顺序

1. 先执行 `create.sql`（建库建表）
2. 再执行 `sample.sql`（导入样例数据）

## 数据库连接信息（本地开发）

```
host:     localhost
port:     3306
database: heritage_spread_db
charset:  utf8mb4
```

用户名和密码各自用自己本地 MySQL 的账号。
