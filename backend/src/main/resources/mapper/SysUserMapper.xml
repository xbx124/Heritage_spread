<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.heritage.mapper.SysUserMapper">

    <select id="selectByUsername" resultType="org.example.heritage.pojo.entity.SysUser">
        SELECT
        user_id AS userId,
        username,
        password,
        role,
        status,
        create_time AS createTime,
        update_time AS updateTime
        FROM sys_user
        WHERE username = #{username}
        LIMIT 1
    </select>

</mapper>
